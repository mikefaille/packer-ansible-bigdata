# from github, get recommended os configs for cdh. Note : Since we use ansible-galaxy, we must add role in git's root folder. If anyone have a suggestion, contact me plz : <michael at faille io>
- src: https://github.com/mikefaille/ansible-static-requirements-bigdata
  name: packer-ansible-bigdata
  version: master
  path: roles/bigdata
